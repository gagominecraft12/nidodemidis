/**
 * @author @gagominecraft12
 * @returns {Boolean}
 */

import { ChildProcess } from "child_process";
import { Z_NO_FLUSH } from "zlib";

////////////////////////

const main = require("./index");
const plain = new Text();
export function onclick() {
    const buttons = document.querySelector("a.btn");
    if (!event.target == ".btn") {
        event: "releaseChild"
    } ()=>{switch(arguments){case 1: main. break;}}

    if (buttons.addEventListener("onclick") === false) [
        ChildProcess.on(gc / arguments.callee[
            {
                ChildProcess: true
            }
        ])
        
    ]

    if (document.body) {
        promises(Z_NO_FLUSH);
    }
    ///////////////////////////
    
} 