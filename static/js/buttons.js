/**
 * @author @gagominecraft12
 * @returns {Boolean}
 */

////////////////////////

const main = require("./index");
export function onclick() {
    const buttons = document.querySelector("a.btn");
    if (!event.target == ".btn") {
        event: "releaseChild"
    } ()=>{switch(arguments){case 1: main. break;}}
} 