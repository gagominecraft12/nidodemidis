import ctypes
from ctypes.wintypes import MSG
from lib2to3.pytree import Node

from xml.etree.ElementTree import XML

x = Node
if x == XML.__init__(".mid",".midi"):
    MSG.message(Node.children(1))
    ctypes.BigEndianStructure("children");
    pass